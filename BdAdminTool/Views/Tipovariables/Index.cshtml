@model IEnumerable<WebAdminbdTool.Models.Tipovariables>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreTipoVariable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaInicial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFinal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdVariableNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoVariable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVariableNavigation.NombreVariable)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoVariable">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoVariable">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoVariable">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
